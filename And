<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>*And* Logical Operator</title>
</head>
<body>
    <h1>Check the Console</h1>
    <script src="AndLogicalOperator.js"></script>
</body>
</html>

//---------------------------------------------------------------------------

const num = 5;
const set = 7;

const password = ("Enter a number");

    if (num > 4 && set < 6) {
        console.log("Only num is greater then 4")
    }

    else if(num > 4 && set > 6) {
        console.log("Both variables are greater");
    }
